const { log, addQuery, parseAttachmentInfo, sizeParse } = require('./helpers')

describe('helpers.js', () => {
    describe('log', () => {
        test('test', () => {
            expect('a').toBe('a')
        })
    })

    describe('addQuery', () => {
        test('test', () => {
            expect('a').toBe('a')
        })
    })

    describe('parseAttachmentInfo', () => {
        test('test', () => {
            expect('a').toBe('a')
        })
    })

    describe('sizeParse', () => {
        test('test', () => {
            expect('a').toBe('a')
        })
    })
})
